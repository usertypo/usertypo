# Cloudflare Pages static site.
# Dashboard build command: npm run build:cloudflare
# Dashboard output directory: dist
# Do NOT add [assets] here — Pages rejects that key and fails the build.

name = "usertypo"
compatibility_date = "2026-07-27"
pages_build_output_dir = "dist"
